let admin = npub1yxprsscnjw2e6myxz73mmzvnqw5kvzd5ffjya9ecjypc5l0gvgksh8qud4;
let mods = admin^;
let members = admin^^;

posts {
    drop if flagged by mods;

    +1 if liked by members;

    /10 if content ~ /bitcoin|btc|crypto/i;
    *3  if content ~ /#grownostr/i;
}

comments {
    drop if flagged by mods;
}
