launch
  #[0] tags in message body, or nostr:npub1 tags of NIP-27
  export jsonl, rss
  user comments screen
    highlight user's name in green or something to call attention to their comments
    ? pagination
  user screen
    ? followers
  thread viewer screen
    algos
  posting
    upvote/downvote/flag buttons
  perf: don't send requests to threads while they are busy
  deflate
  styling


edit profile
